* text=auto

# Текстовые файлы — коммитим с LF
*.{js,jsx,ts,tsx,css,scss,md,html,json,yml,yaml} text eol=lf
*.sh text eol=lf

# Бинарные — не трогаем переводы строк
*.{png,jpg,jpeg,gif,ico,svg,webp,woff,woff2,ttf,otf,zip} binary
